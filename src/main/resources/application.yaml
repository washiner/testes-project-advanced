spring:
  application:
    name: testes

  datasource:
    url: jdbc:postgresql://localhost:5432/testes_db
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
